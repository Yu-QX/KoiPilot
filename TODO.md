# Things Planning to Do

### Features
- Convert `FileManager` to new module specifically for MCP toolkit. *(Due by 2025-12-01)*

### Structure
- Move all default values to new module; and seperate user data from scripts. *(Date unplanned)*
- Transfer `requests` to `urllib`, and make sure no third party modules are used. *(Date unplanned)*